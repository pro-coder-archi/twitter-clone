version: "2"

sql:
    - engine: mysql
      queries: ./backend/microservices/authentication/sql/queries.sql
      schema: ./backend/microservices/authentication/sql/migrations
      gen:
        go:
            package: repository
            out: ./backend/microservices/authentication/repository
            emit_interface: true